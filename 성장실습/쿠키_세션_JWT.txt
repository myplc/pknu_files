프론트앤드 : 리액트  -  쿠키  ,  막아요.(JS)
헤더 쿠키를 담는다

백앤드 : 스프링부트 - 세션(유지)- 로그아웃 -> 쿠키X
쿠키없다? -> 쿠키만들어준다(유효기간등) ->프론트
스프링부트에서 함수() 쿠키정보를 읽어오기

세션식 - > 목록검사 (서버에는 목록리스트가 있어야 함)

JWT 최소화
장점: 입장정보, 유효기간등만 간소화 -> 가볍고 빠르다.
단점: 탈취되면 큰일남..
대책은 ->
1) 블랙리스트 만들면 되는데 이러면 세션이랑 다를게 없음;;;
2) 유효기간 짧게 만듬, 귀찮아 질수도 있지만(재발급) 보안은 증가.

